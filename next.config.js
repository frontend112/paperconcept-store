/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "placehold.co",
      "dl.dropboxusercontent.com",
      "paperconcept.pl",
    ],
  },
};

module.exports = nextConfig;
